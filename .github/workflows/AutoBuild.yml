name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build Excel And Proto
      run: 
      - cd PublicTools 
      - sh runbuild.sh
    #- name: build Server
    #  run: sh buildforci.sh  
    
